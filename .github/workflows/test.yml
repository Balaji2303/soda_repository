name: Scan for data quality

on: pull_request

jobs:
  soda_scan:
    runs-on: ubuntu-latest
    name: Run Soda Scan
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Perform Soda Scan
        uses: sodadata/soda-github-action@v1.0.2
        with:
          soda_library_version: v1.5.5
          data_source: snowflake
          configuration: ./CONFIGURATION.yml
          checks: ./CHECKS.yml
        env:
          SODA_CLOUD_API_KEY: d7be935f-4f06-40a1-b77a-f4ec5207f115
          SODA_CLOUD_API_SECRET: Br7TazObPMhPXIlC-wJN7axtZiWxojOhBXeCNsT98q4Jc88MKD_owA
          SNOWFLAKE_USERNAME: balajisanthanam
          SNOWFLAKE_PASSWORD: Bala_ji_2303
